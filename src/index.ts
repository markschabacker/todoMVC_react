import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { TodoRoot } from './components/TodoRootComponent';

ReactDOM.render(React.createElement(TodoRoot), document.getElementById('todoapp'));
