import * as React from 'react';
import * as ReactDOM from 'react-dom';

import { TodoApp } from './components/TodoAppComponent';

ReactDOM.render(React.createElement(TodoApp), document.getElementById('todoapp'));
